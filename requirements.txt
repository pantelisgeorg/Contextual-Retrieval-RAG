openai>=1.30.0
chromadb>=0.5.0
ollama>=0.2.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
pymupdf>=1.24.0
docling==2.108.0
tqdm>=4.66.0
python-dotenv>=1.0.0
numpy>=1.26.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- torch stack (CUDA 12.1) ---
# Pinned for Pascal GPUs (GTX 1070 Ti, compute capability 6.1 / sm_61).
# Newer torch (>=2.6) dropped sm_61 from its prebuilt CUDA kernels.
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
timm==1.0.19
